pipeline {
    agent any

    stages {

        stage('Checkout Success') {
            steps {
                echo 'Repository connected successfully!'
            }
        }

        stage('List Files') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }

    }
}
